# HANDOFF — GLM-5.2 AWQ single-node work (2026-07-03)

Continuation state for a fresh context. Deep findings live in memory
(`project_glm52_status.md`); this file is the **immediate actionable state**.
Worktree: `/Users/bdbrodie/dev/sigma2/olivia/add-glm52-awq` (branch `add-glm52-awq`).
Cluster access: `ssh -o ControlPath=~/.ssh/controls/olivia-bdbrodie@olivia bdbrodie@olivia`
(reuse the master; if down, ASK USER to `./olivia.sh reconnect` in a real terminal — needs 2FA).

## RIGHT NOW: runai_streamer test in flight — THE (c) WIN
- **Job 1472308** (`vllm-glm52runai`) on gpu-1-108: glm52_awq_1n + `LOAD_FORMAT=runai_streamer`
  + `CONTAINER_PYTHONPATH=/cluster/projects/nn10104k/huggingface/runai-pkg`, offload40/fp8_ds_mla/131K.
- **RESULT SO FAR: runai loads ~6× faster** — "Loading safetensors using Runai Model Streamer"
  at ~450 it/s, ETA ~8.5 min total load **vs ~56 min default**. This confirms the load was
  vLLM-pipeline-bound, not I/O-bound (raw Lustre read is ~1.3 GB/s; page-cache reuse was a dead end).
- **Pending:** background check `b1drvma7m` (a `sleep 480; grep` of the log) will report the exact
  `Model loading took` + whether it reaches `Application startup complete` (serves cleanly).
- Log: `/cluster/work/projects/nn10104k/containers/logs/vllm_server_1472308.log`. 2h walltime.

### FIRST THING TO DO on resume
1. Read `b1drvma7m` output (or `grep -E "Model loading took|Application startup complete|Traceback" <log>`).
2. If it SERVES: runai_streamer is validated. Optionally quick-decode-test it (tunnel: `SERVER_JOB_NAME=vllm-glm52runai ./olivia.sh chat --tunnel-only` then `.venv/bin/python chat_devstral.py localhost --port 8003 --stream`), then **scancel 1472308** (stop the bleed).
3. If it FAILED at import/serve: check the log; likely a missing pure-python dep (stage it into
   `/cluster/projects/nn10104k/huggingface/runai-pkg`) or a runai/vLLM version quirk.

## UNCOMMITTED CHANGES (stage + commit once runai confirmed)
- `run_vllm_server.sh`: two new knobs — `LOAD_FORMAT` (→ `--load-format`) and `CONTAINER_PYTHONPATH`
  (→ container `--env PYTHONPATH`, for the staged runai pkg). Both near the VLLM_ARGS / SING_CMD binds.
- `olivia.sh`: forwards `LOAD_FORMAT CONTAINER_PYTHONPATH` (added to the env forward list).
- Nothing else uncommitted. `bash -n` clean on both.
- **If runai validated → make it the DEFAULT for GLM-5.2** in run_vllm_server.sh: when IS_GLM52 and
  the runai pkg exists, default `LOAD_FORMAT=runai_streamer` + `CONTAINER_PYTHONPATH=$HF_HOME/runai-pkg`
  (guard so it's overridable / no-ops if pkg missing). Then commit: "feat(glm52): runai_streamer loader
  (~6x faster cold load) + LOAD_FORMAT/CONTAINER_PYTHONPATH knobs".

## runai package staging (durable, already done)
- aarch64 wheel + humanize extracted to `/cluster/projects/nn10104k/huggingface/runai-pkg/`
  (under HF_HOME so it's bound in the container; loaded via PYTHONPATH — no container modification).
- Source wheels cached at `/cluster/projects/nn10104k/.mtp-graft/runai-wheels/`.
- To re-stage: login node has internet; `pip download runai-model-streamer --no-deps --only-binary=:all:
  --platform manylinux2014_aarch64 --python-version 3.12 --abi cp312 --implementation cp`, unzip under HF_HOME.

## PRIORITY QUEUE (agreed with user)
1. **(c) load-time — runai_streamer** ← IN PROGRESS, validating now. On success: default it + commit. DONE-ish.
2. **(b) KV-tiering (LMCache)** ← NEXT. Bulk KV in Grace LPDDR, hot pages in HBM → serve many concurrent
   long-context sessions on one node. LMCache as a vLLM KV connector (keeps GLM-5.2 support). Big
   integration bet; unproven with MLA/DSA. This needs a new job allocation + research.
3. **(a) 2-node AWQ, no offload (diagnostic)** — weights fully in HBM (415 GiB fits 8×96), removes the
   C2C tax → measures the true decode ceiling. 2× GPU cost + PP wedge.
4. (d) low-offload no-MTP — lowest priority; attacks C2C tax single-node (conflicts with MTP on HBM).

## KEY RESULTS ALREADY BANKED (committed: a60140d, 3fb5710, 3d77ccb, 2be2aa0)
Peak single-node config = **capture + MTP + fp8_ds_mla + offload40 + 131K**: ~22 tok/s single-stream
(3.2× eager), ~241 tok/s @64-way, 0 fail. 3× throughput at 1/3 GPU cost of the multi-node path.
Presets on branch: `glm52_awq` (2-node), `glm52_awq_1n` (single-node offload), `glm52_awq_mtp`
(single-node + MTP, model = the grafted checkpoint `/cluster/projects/nn10104k/models/GLM-5.2-AWQ-INT4-MTP-FP8`).
Full sweep table + all findings in `plans/proposed/glm52_awq_1n_offload.md`.

## OPERATIONAL GOTCHAS (bit us this session)
- **Bash tool: never put `(parens)` in `echo` inside `ssh 'bash -lc "..."'`** — breaks the shell. Recurring.
- **SSH master drops on laptop sleep** during long idle waits → my pollers die (exit 255 / killed). The
  cluster-side watcher pattern (`/cluster/projects/nn10104k/.mtp-graft/watch_bench.sh`, detached via
  setsid) is immune — use it for anything that must survive a disconnect. `bench_sweep.py` staged there too.
- **Compute nodes have NO internet** (DNS fails) — only login nodes do. Use `HF_HUB_OFFLINE=1` or the
  cluster's squid proxy. That's why runai deps must be staged from a login node.
- **`salloc` needs `--account=nn10104k`.** Manual `srun`-into-`salloc` for vLLM is FRAGILE (proxy/offline
  env + GRES leftovers) — prefer the sbatch flow (`olivia.sh server start`). Don't rabbit-hole on it.
- **MTP double-loads** (~84 min: main ~56 + drafter ~29) because it re-reads the whole checkpoint for the
  draft model. runai_streamer should slash this too.
- accel is often saturated (queue waits hours); idle windows come and go — grab them. GPU budget was ~514/2000 hr.
- The `cluster`/`cluster --watch` view now shows single-node LOADING % + `[cg=PIECEWISE(cap)]` + `(drafter/MTP)`
  (fixed this session in olivia.sh, committed 2be2aa0).
